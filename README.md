# list_deviantart_urls
A simple Python scraper that collects all artwork URLs from a DeviantArt user’s public gallery. It crawls each gallery page, extracts every /art/ link, and outputs a clean list of URLs for backup, indexing, or portfolio organization. Works without API keys and runs in any Python virtual environment.
